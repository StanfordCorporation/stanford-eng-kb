# Runtime deps for the Vercel Python function (api/index.py + backend/**).
# Anything that is ONLY used by local entry points (uvicorn dev server,
# expose_mcp, the bulk-upload CLI) lives in requirements-dev.txt instead, so
# the deployed function bundle stays under Vercel's 250 MB cap.

psycopg2-binary>=2.9.9
pgvector>=0.3.0
python-dotenv>=1.0.1
langchain-text-splitters>=0.3.0
openai>=1.40.0
anthropic>=0.39.0
fastapi>=0.115.0
python-multipart>=0.0.9
pypdf>=4.0.0
python-docx>=1.1.0
